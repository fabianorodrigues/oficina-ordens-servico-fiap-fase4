apiVersion: v1
kind: Service
metadata:
  name: oficina-ordens-servico
  namespace: oficina
spec:
  type: ClusterIP
  selector:
    app: oficina-ordens-servico
  ports:
    - name: http
      port: 80
      targetPort: http
